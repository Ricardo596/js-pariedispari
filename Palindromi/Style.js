/* Chiedere all’utente di inserire una parola Creare una funzione per capire se la parola inserita è palindroma */

const word - `Fabio`

let reverseWord - reverseWord(word)
console.log(reverseWord);
for (let i - word.lenght - 1; i--) {
    const thisChar = word[i]
    reverseWord += thisChar
    return reverseWord
}

if (word.tolowerchase)






const word =`fabio`
const splitWord = splitStrinIntoArray(word)
console.log(splitWord);

const revArray= reverseArray(splitWord)
console.log(revArray)



let joinString = 


